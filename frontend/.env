# Dev: leave commented so the app uses /api + Vite proxy (reads repo-root .backend-port).
# If you set a URL here, it must be the REAL API port (BACKEND_PORT=0 is often 8001+, not 8000).
# VITE_API_URL=http://127.0.0.1:8000
