# AI Sales Agent — backend (Python 3.11+)
# Install: pip install -r requirements.txt

# --- Web API & server ---
fastapi>=0.109.0,<0.116.0
uvicorn[standard]>=0.27.0,<0.35.0
python-multipart>=0.0.9

# --- Data & validation ---
sqlalchemy>=2.0.25,<2.1.0
pydantic[email]>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0

# --- Auth & security ---
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0

# --- HTTP client (Ollama / OpenAI) ---
httpx>=0.26.0,<0.29.0

# --- Config ---
python-dotenv>=1.0.0

# --- Tests (CI/local) ---
pytest>=8.0.0,<9.0.0

# --- Optional: PostgreSQL in production ---
# Uncomment on the server if DATABASE_URL uses postgresql://...
# psycopg2-binary>=2.9.9
